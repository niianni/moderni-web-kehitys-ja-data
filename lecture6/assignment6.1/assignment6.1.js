const car = {
  make: "Ford",
  model: "Focus",
  color: "green", 
  year: 2010
};

console.log(`I have a ${car.color} ${car.make} ${car.model}. It was made in the year ${car.year}.`);

car.color = "orange";

console.log(`I have a ${car.color} ${car.make} ${car.model}. It was made in the year ${car.year}.`);

